server:
  port: 8083

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
      - org.springframework.boot.actuate.autoconfigure.security.servlet.ManagementWebSecurityAutoConfiguration
  datasource:
    url: ${LIGHTING_DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${LIGHTING_DB_USER:lighting}
    password: ${LIGHTING_DB_PASSWORD:lighting}
    driver-class-name: ${LIGHTING_DB_DRIVER:org.postgresql.Driver}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  sql:
    init:
      mode: always
      encoding: UTF-8

lighting:
  config:
    mode: embedded
    auth:
      enabled: true
      mode: token
      options:
        tokens: lighting-config-token
    embedded:
      storage:
        type: FILE
        path: ./example-config.json

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
