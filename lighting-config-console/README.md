# lighting-config-console (WIP)

前端控制台项目占位目录，详见 `docs/frontend-architecture.md`。

## 状态
- 尚未初始化 Vite 工程，待 Architect/Frontend Agent 批准方案后再执行。
- 预期技术栈：Vite + React + TypeScript + Ant Design（参考架构文档）。

## 下一步
1. `pnpm create vite lighting-config-console --template react-ts`。
2. 接入 TanStack Query、Ant Design UI、统一的 API SDK。
3. 与 `lighting-config-server` 的 `/api` 端点联调，完善登录/鉴权流程。
